{
  "name": "conformance",
  "version": "0.0.0",
  "private": true,
  "description": "One suite, two hosts: every floor case runs against demo-cli-commander and demo-cli-yargs. The contract between commander-agent and yargs-agent (design R6 of cli-testing-harness).",
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "dependencies": {
    "@interlace/cli-core": "*",
    "commander-harness": "*",
    "demo-cli-commander": "*",
    "demo-cli-yargs": "*",
    "yargs-testing": "*"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  }
}
